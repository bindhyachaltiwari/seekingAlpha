const defaultWidth: number = 50;
const defaultHeight: number = 50;
const defaultTime: number = 400; //ms

const defaultClassNames = {
  OFF: 'off', ON: 'on',
}

export { defaultWidth, defaultHeight, defaultTime, defaultClassNames }